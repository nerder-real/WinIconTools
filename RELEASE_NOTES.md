# v1.0.0 (2026-09-14)

首个公开版本。

## 功能

- **隐藏快捷方式小箭头**：方案一 `explorer.exe,-264`（推荐）/ 方案二 `Taskbar.dll,-264` / 方案三 `blank.ico`（备选）/ 自定义方案参数查看，当前生效方案自动高亮
- **隐藏「- 快捷方式」后缀**：`Link = 00000000` 方案（仅对新建快捷方式生效），支持查看参数与一键恢复
- **隐藏 UAC 小盾牌**：方案一 `blank.ico`（推荐，无残留边框）/ 方案二 `shell32.dll,-50` / 方案三 `explorer.exe,-264` / 自定义方案参数查看
- **恢复系统默认**：小箭头 / 快捷方式后缀 / UAC 盾牌 独立一键恢复
- **系统实用工具**：Win10 经典右键菜单 ⇄ Win11 新版右键菜单 切换（支持强删 HKLM 中 TrustedInstaller 所有的 CLSID 键）、重建桌面图标缓存（含 20 秒看门狗）

## 界面

- 无边框圆角窗口，深色/浅色模式跟随系统（2 秒内自动换肤）
- 状态卡：四行实时状态（名称 + 彩色状态点 + 右侧徽标），执行中对应行显示「正在执行…」
- 方案按钮按分区着色 + 渐变高亮，哪个生效哪个亮
- 标题栏自定义绘制：高清 LOGO + 署名

## 技术

- C# 5 / WinForms，Windows 自带 csc 编译，无第三方依赖
- 单文件自包含（blank.ico、logo64.png 内嵌），双击即用
- UAC 清单自动提权；注册表操作实时反映到状态卡

## 附带

- `legacy/cmd/`：与 GUI 功能一一对应的 5 个独立批处理脚本
- `legacy/快捷方式小箭头.cmd`：最早的合并版脚本（存档）
